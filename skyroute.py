from graph_search import graph_search
from vc_metro import vc_metro
from vc_landmarks import vc_landmarks
from landmark_choices import landmark_choices
#landmark_string is a string connecting all of the landmarks together, each with its corresponding letter of the alphabet from landmark_choices on a new line.
landmark_string = "" #as a string, we set it with nothing inside.
#create a landmark_string:
for letter,landmark in landmark_choices.items() 
